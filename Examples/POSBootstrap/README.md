# POS local bootstrap

Installs a minimal POS stack (drivers/certs/tools) using Update Service client cmdlets.

Example:

```powershell
./Install-PosStack.ps1 -InstanceName POS -PackageIds epson-opos-adk,toshiba-upos
```
